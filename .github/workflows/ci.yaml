name: Calculator CI

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  replace-me-later:
    name: Dummy Job
    runs-on: ubuntu-latest
    steps:
      - name: Say Hello
        run: echo "Hello World"
